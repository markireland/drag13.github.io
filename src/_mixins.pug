mixin taglist(tags)
 div.tags
   each tag in tags
     span ##{tag} 

mixin header(header)
  if (header)
    h2.header=header
  else
    h2.header
      block

mixin pp-body(text, href, link)
  p=text
  if href
    p
      if link
        a(href=href)=link
      else
        a(href=href) Continue

mixin pp(tags, header, text, href, link)
  div.pp
    +taglist(tags)
    +header()
      a.text(href=href) #{header}
    +pp-body(text, href, link)

mixin news(tags, header)
  div.pp
    +taglist(tags)
    +header(header)
    block